{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@prisma/client": "5",
    "better-auth": "^1.6.20",
    "express": "^5.2.1",
    "prisma": "5"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.0.1",
    "typescript": "^6.0.3"
  }
}
