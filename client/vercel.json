{
  "installCommand": "cd .. && bun install",
  "buildCommand": "cd .. && bun run --filter client build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
